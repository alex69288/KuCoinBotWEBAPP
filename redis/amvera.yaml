meta:
  environment: docker
  toolchain:
    name: docker
    version: latest

build:
  context: redis
  dockerfile: Dockerfile

run:
  containerPort: 6379
  port: 6379
  persistenceMount: /data
  command: redis-server --appendonly yes