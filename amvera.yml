meta:
  environment: node
  toolchain:
    name: npm
    version: 20
  source:
    path: backend

build:
  additionalCommands: npm run build

run:
  command: npm run start
  containerPort: 5000
  persistenceMount: /data