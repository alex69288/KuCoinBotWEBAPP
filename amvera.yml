meta:
  environment: node
  toolchain:
    name: npm
    version: 20
  source:
    path: backend

build:
  additionalCommands: npm run build

run:
  command: node dist/index.js
  containerPort: 5000
  persistenceMount: /data