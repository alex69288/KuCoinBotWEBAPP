meta:
  environment: node
  toolchain:
    name: browser
    version: 20
  source:
    path: frontend

build:
  installCommand: npm install
  additionalCommands: |
    cd frontend && npm install
    npm run build

run:
  command: npm run preview
  containerPort: 4173