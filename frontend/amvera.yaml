meta:
  environment: node
  toolchain:
    name: browser
    version: 20

build:
  additionalCommands: npm run build

run:
  command: npm run preview
  containerPort: 4173

run:
  runCommand: npm run preview
  containerPort: 4173