meta:
  environment: node
  toolchain:
    name: browser
    version: 20

build:
  installCommand: npm install
  buildCommand: npm run build

run:
  runCommand: npm run preview
  containerPort: 4173

run:
  runCommand: npm run preview
  containerPort: 4173