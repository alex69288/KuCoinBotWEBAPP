meta:
  environment: node
  toolchain:
    name: browser
    version: 20

build:
  installCommand: npm install
  additionalCommands: |
    cd frontend && npm install
    cd frontend && npm run build

run:
  command: npm run preview
  containerPort: 4173