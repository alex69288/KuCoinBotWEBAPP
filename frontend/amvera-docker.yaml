meta:
  environment: docker
  toolchain:
    name: docker
    version: latest

build:
  dockerfile: Dockerfile

run:
  containerPort: 80
  port: 80
  env:
    - VITE_API_URL=https://kucoinbot-backend-docker.amvera.io/api