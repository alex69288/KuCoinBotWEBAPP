meta:
  environment: docker
  toolchain:
    name: docker
    version: latest

build:
  context: .
  dockerfile: Dockerfile

run:
  containerPort: 8080
  port: 443
  command: npm start
  env:
    - NODE_ENV=production
    - PORT=8080