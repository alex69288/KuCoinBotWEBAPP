meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: npm run build
  persistenceMount:
    - path: /data
      size: 1Gi

run:
  command: npm run start
  containerPort: 5000